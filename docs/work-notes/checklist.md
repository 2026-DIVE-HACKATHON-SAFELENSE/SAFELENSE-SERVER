# Kotlin Spring Boot 설정 체크리스트

- [x] 기존 저장소와 로컬 JDK 환경을 확인한다.
- [x] Kotlin, Spring Boot, MySQL/JPA 기반과 최소 의존성을 결정한다.
- [x] Gradle Kotlin DSL 설정과 실패하는 애플리케이션 진입점 테스트를 추가한다.
- [x] Gradle Wrapper를 생성하고 테스트의 실패 원인을 확인한다.
- [x] Kotlin Spring Boot 진입점을 추가한다.
- [x] 전체 테스트와 실행 JAR 생성을 검증한다.
- [ ] MySQL 환경 변수를 제공받아 DB 연결 기동을 검증한다.
- [ ] 변경 사항을 검토하고 커밋한다.
- [x] 리프레시 토큰 검증과 액세스 토큰 재발급을 구현한다.
- [x] 토큰 재발급 API를 전체 테스트하고 커밋한다.
- [x] 리프레시 토큰을 MySQL에 저장하고 갱신 시 교체한다.
- [x] 액세스 JWT 기반 로그아웃 API를 구현한다.
- [x] 내 집 등록·부분 수정 API 설계와 구현 계획을 확정한다.
- [x] V3 내 집 정보 마이그레이션을 테스트 우선으로 추가한다.
- [x] 사용자별 내 집 조회·생성·부분 수정 서비스를 구현한다.
- [x] GET·POST·JSON Merge PATCH API와 오류 응답을 구현한다.
- [x] 내 집 관련 테스트, 전체 테스트와 실행 JAR 생성을 검증한다.
- [x] 변경 사항을 검토하고 의미 단위로 커밋한다.
- [x] 노션 분석 케이스 명세와 사진의 화면 흐름을 대조한다.
- [x] 부분 입력 분석과 입력 근거 없음 시 `UNKNOWN` 반환을 확정한다.
- [x] 연습 데이터 기반 규칙 분석과 분석 케이스 API를 설계한다.
- [x] 1차 구현 범위를 분석 실행을 제외한 입력 API 6개로 축소한다.
- [x] 분석 케이스 상세 구현 계획을 작성한다.
- [x] 분석 케이스·문서·답변 마이그레이션을 테스트 우선으로 추가한다.
- [x] 단계별 템플릿과 사용자별 분석 케이스 API를 구현한다.
- [x] 문서 업로드·삭제와 체크리스트 저장 API를 구현한다.
- [x] 분석 케이스 관련 테스트, 전체 테스트와 실행 JAR 생성을 검증한다.
- [x] 변경 사항을 검토하고 의미 단위로 커밋한다.
- [x] 체크리스트 전체 교체가 명시적 bulk DELETE를 삽입보다 먼저 실행하도록 회귀 테스트와 구현을 보강한다.
- [x] 체크리스트 미소유 케이스, 빈 교체, 빈 `itemKey` 인접 계약을 테스트한다.
- [x] 케이스 상세 조회가 문서 메타데이터 projection만 사용하도록 테스트와 구현을 보강한다.
- [x] 문서 삭제가 케이스 범위 bulk DELETE의 삭제 행 수로 오류를 구분하도록 테스트와 구현을 보강한다.
- [x] 문서 삭제의 케이스·문서 미존재 오류 계약을 설계 문서와 MVC 테스트에 일치시킨다.
- [x] 실제 MySQL·multipart resolver 통합 검증의 보류 사유를 기록한다.
- [x] 최종 리뷰 수정의 집중 테스트, 전체 테스트, 실행 JAR와 공백 검사를 검증한다.
- [x] 최종 리뷰 수정 사항을 자체 검토하고 의미 단위로 커밋한다.
- [x] 연습 데이터 확보 후 위험 분석 API를 별도 작업으로 설계한다.
- [x] 노션 알림 명세와 사진의 화면 요구사항을 대조한다.
- [x] 목록·한 건 읽음·모두 읽음·전체 미읽음 개수의 설계를 확정한다.
- [x] 알림 기능 구현 계획을 작성한다.
- [x] V5 알림 마이그레이션을 테스트 우선으로 추가한다.
- [x] 내부 알림 생성과 입력 검증을 구현한다.
- [x] 사용자별 커서 목록과 전체 미읽음 개수를 구현한다.
- [x] 한 건 읽음과 모두 읽음의 멱등 처리를 구현한다.
- [x] 알림 HTTP API와 오류 응답을 구현한다.
- [x] 알림 집중 테스트, 전체 테스트와 실행 JAR 생성을 검증한다.
- [x] 변경 사항을 최종 검토하고 의미 단위로 커밋한다.
- [x] 미완료 API와 분석 실행 제외 범위를 확정한다.
- [x] 분석 이력·상세·PDF와 사용자 API 설계를 작성한다.
- [x] 분석 결과 V6 마이그레이션과 영속 모델을 테스트 우선으로 추가한다.
- [x] 분석 이력 목록과 상세 조회를 구현한다.
- [x] 저장된 분석 결과의 PDF 리포트 생성을 구현한다.
- [x] 내 정보 조회와 온보딩 상태 변경을 구현한다.
- [x] 관련 테스트, 전체 테스트와 실행 JAR 생성을 검증한다.
- [x] 변경 사항을 검토하고 의미 단위로 커밋한다.
- [x] `feat/report`를 `main`에 병합하고 `origin/main`에 푸시한다.
- [x] DIVE CSV 7개와 XLSX 1개의 구조·분포·결합 가능성을 분석한다.
- [x] 사고 확률이 아닌 버전형 위험 신호 점수로 분석 실행 API 설계를 확정한다.
- [x] `feat/analyze-logic` 브랜치를 만들고 전체 테스트 기준선을 확인한다.
- [x] 위험 분석 실행 설계 문서와 구현 계획을 작성한다.
- [x] V7 분석 입력 스냅샷·멱등 키 마이그레이션을 테스트 우선으로 추가한다.
- [x] `dive-2026-v1` 위험 규칙 엔진을 테스트 우선으로 구현한다.
- [x] 분석 실행 서비스와 `POST /api/v1/analysis-cases/{caseId}/analyze`를 구현한다.
- [x] 분석 완료 케이스의 문서·체크리스트 변경을 차단한다.
- [x] 분석 집중 테스트, 전체 테스트, 실행 JAR와 공백 검사를 검증한다.
- [x] 전체 변경 사항을 하나의 의미 단위 커밋으로 저장한다.
- [x] 허용 출처 3개의 CORS 정책을 설계하고 명세를 검토받는다.
- [x] CORS 사전 요청의 허용·비허용 출처 테스트를 추가한다.
- [x] Spring Security CORS 설정과 애플리케이션 출처 목록을 구현한다.
- [x] CORS 집중 테스트와 전체 테스트를 검증한다.
- [x] 변경 사항을 검토하고 커밋한다.
- [ ] prod SSM 선행 로딩, PostgreSQL 전환, OIDC Run Command 배포 설계를 기록한다.
- [ ] SSM 로더와 PostgreSQL 마이그레이션의 실패 테스트를 추가한다.
- [ ] AWS SDK SSM 로더와 PostgreSQL 의존성·스키마 전환을 구현한다.
- [ ] S3 아티팩트·systemd·OIDC Run Command 배포 정의와 최소 IAM 정책을 추가한다.
- [ ] 집중 테스트, 전체 테스트, 실행 JAR와 배포 정의를 검증한다.
- [ ] 변경 사항을 검토하고 main에 병합·푸시한다.
